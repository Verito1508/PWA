server:
  port: 8080
spring:
  application:
    name: gateway-service

# Config del gateway
gw:
  backends:
    - http://coordinator-service-pwa
    - http://coordinator-service-pwa   # S2 (puedes repetir o usar otra instancia si la tuvieras)


  requestTimeoutMillis: 30000

management.endpoints.web.exposure.include: health,info
